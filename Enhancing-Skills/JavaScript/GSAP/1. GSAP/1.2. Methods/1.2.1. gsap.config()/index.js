// gsap.config() Method

// Type: Method

//  gsap.config() lets you configure GSAP's settings that aren't Tween-specific, like autoSleep, force3D and units. To affect properties that should be inherited by individual tweens, use gsap.defaults() instead.

