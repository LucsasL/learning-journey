// 1. Store this in a template string ~
// The quick
// brow fox
// jumps over
// the lazy dog
const templateString = `The quick
brow fox
jumps over
the lazy dog`;

// 2. Create two variables (firstName, lastName)
const firstName = `Lucas`;
const lastName = `Lira`;

// 3. Use those variables in template string.
console.log(`My name is: ${firstName} ${lastName} and I saw...
${templateString}`);