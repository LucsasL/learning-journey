// Annotations are used to specify the data type of a variable, parameter, function return value, and other types of values. Annotations help developers catch errors early in development by allowing them specify what types of values can be assigned to a given variable or passed as an argument to a function.

// Strings

// let myVar: type = value
let myName: string = "Irineu";
myName = "Other Person";
console.log(myName);

// ERROR
// myName = 12;

// Numbers
let favNumber: number = 5;
console.log(favNumber);

// Booleans
let isHard: boolean = false;
isHard = true;
console.log(isHard);