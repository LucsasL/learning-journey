'use client';

import React from 'react';

function Client() {
  return (
    <div>
      <button onClick={() => alert("Opa")}>
        Click Me
      </button>
    </div>
  );
}

export default Client;