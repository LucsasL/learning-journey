// 1. Create array with random numbers.
const numbers = [1, 2, 3, 4, 5];

// 2. Map over each items in that array, & multiply each item by 10
const results = numbers.map(num => num * 10);

console.log(results);